apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dcc-codex-tls
  namespace: dcc-codex
spec:
  secretName: dcc-codex-tls
  issuerRef:
    name: step-issuer
    kind: StepClusterIssuer
  duration: 2160h  # 90 days
  renewBefore: 360h  # 15 days
  dnsNames:
    - dcc-codex.home
